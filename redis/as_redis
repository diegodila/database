1.1 o que é o redis? 
é um banco de dados em memoria

1.2 qual a principal vantagem do redis?
imagine que voce quer saber a quantidade de usuarios online, toda vez voce teria que fazer uma query e processar esses dados para saber, se fosse muitos clientes,seriam muitas consultas. o redis guarda os dados em memoria

1.3 como eu guardo os dados em memoria no Redis?
setando valores em memoria
usando o comando SET usando a chave e o valor a ser guardado

1.4 coloque quantos usurios online no redis (ficticio)
SET "usuarios_online" 50000

1.5 como eu pego os dados do Redis?
com GET

1.6 como eu pego a quantidade de usuarios_online no Redis?
GET "usuarios_online"

1.7 qual é o tipo de dados que o Redis retorna com o GET?
string

1.8 como faço para atualizar um dados no redis?
com o SET e a chave do dado que deseja atualizar

1.9 como faço para deletar um dado no redis?
DEL e chave do dado 

1.10 o que acontece se o servidor nao estiver ativo no redis?
ele perde os dados em memoria, os dados sao apagados da memoria do redis

1.11 qual é o resultado que aparece quando nao encontramos o dado em memoria
nil

2.1 como  faço para verificar quais chaves existentes em memoria?
KEYS *

2.2 como faço para trazer as chaves que contem "notas" no começo da chaves no redis?
KEYS nota*

2.3 o que o * (asterisco) faz na busca de algo no redis?
substitui qualquer quantidade de caracteres

5.1 qual é o comando para colocar um elemento no final da fila?
RPUSH

5.2 qual é o comando para colocar um elemento no começo da fila?
LPUSH

5.3 coloque um elemento no final da fila
RPUSH "fila:confirma-celular" "8988988"

5.4 como contar quantos elementos tem num lista?
LLEN {key}

5.5 como ver os elementos de uma lista
LRANGE {key} start stop

5.6 como pegar um elemento pelo index?
LINDEX {key} {index}

5.7 como remover o primeiro elemento da lista?
LPOP {key}

5.8 como remover 3 primeiros elementos da lista?
LPOP {key} 2

5.9 como pegar todos os elementos da lista sem saber a quantidade?
LRANGE {key} start -1

5.10 como remover o ultimo elemento da lista?
RPOP {key}

5.11 qual o problema do busy wait?
imagine que voce tem um sistema1 o redis e sistema cliente do redis
quando o sistema cliente nao tem mais dados a retirar do redis, ele nao pode ficar pedindo requisições sem que haja dados novos


5.12 o que faz o comando BLPOP
ele resolve o problema do busy wait, quando nao tem dados no redis, ele espera

5.13 como armazenar dados da estrutura de dados hash no Redis?
HSET {key} {value} {point}

5.14 como verificar o tipo de dados de uma key?
TYPE {key}

5.15 como pegar todos os dados de uma estrutura de dados hash?
HGETALL {key}

5.16 como incrementar ou somar uma pontuacao dentro de uma estrutura de dados hash
HINCRBY {key} {field} {increment}
ex:HINCRBY "jogador" "diego" 5


5.17 como incrementar ou somar uma pontuacao dentro de uma estrutura de dados string

INCR {keys} {increment}

5.18 como incrementar ou somar uma pontuacao dentro de uma estrutura de dados um float
INCRBYFLOAT {key} {increment} 

5.19 como incrementar ou somar uma pontuacao dentro de uma estrutura de dados de hash um float
HINCRBYFLOAT {key} {increment}

6.1 o que é sorted set? 
é uma estrutura de dados de conjuntos ordenados

6.2 Como inserir dados ordenados?
ZADD {key} {value} {field}

6.3 o que é zset?
sorted set, um conjunto ordenado, uma estrutura de dados 

6.4 o que é zcard?
é a cardinalidade de um sorted set

6.5 como ver os dados de uma sorted set?
ZRANGE {key} 0 -1

6.6 como inverter um sorted set (lista)? 
ZREVRANGE {key} 0 -1  --(reverso)REV

6.7 como verificar qual score de um valor
ZSCORE {key} {member} 

6.8 Como verificar em qual posicao do rank um valor esta?
ZREVRANK {key} {member}

6.9 Qual é a ordenação do sorted set default?
do menor para o maior (ordem crescente)

6.10 como incrementar uma valor dentro de sorted set?
ZINCRBY {key} {increment/field} {member}

 